// This file is autogenerated by the spacedb compiler
/* eslint-disable camelcase */

const { IndexEncoder, c } = require('p2p-spacedb/runtime')
const { version, getEncoding, setVersion } = require('./messages.js')

// '@db/nested-members' collection key
const collection0_key = new IndexEncoder([
  IndexEncoder.STRING
], { prefix: 0 })

function collection0_indexify (record) {
  const a = record.member?.id
  return a === undefined ? [] : [a]
}

// '@db/nested-members' value encoding
const collection0_enc = getEncoding('@db/nested/spacedb#0')

// '@db/nested-members' reconstruction function
function collection0_reconstruct (version, keyBuf, valueBuf) {
  const key = collection0_key.decode(keyBuf)
  setVersion(version)
  const record = c.decode(collection0_enc, valueBuf)
  record.member.id = key[0]
  return record
}
// '@db/nested-members' key reconstruction function
function collection0_reconstruct_key (keyBuf) {
  const key = collection0_key.decode(keyBuf)
  return {
    member: {
      id: key[0]
    }
  }
}

// '@db/nested-members'
const collection0 = {
  name: '@db/nested-members',
  id: 0,
  encodeKey (record) {
    const key = [record.member.id]
    return collection0_key.encode(key)
  },
  encodeKeyRange ({ gt, lt, gte, lte } = {}) {
    return collection0_key.encodeRange({
      gt: gt ? collection0_indexify(gt) : null,
      lt: lt ? collection0_indexify(lt) : null,
      gte: gte ? collection0_indexify(gte) : null,
      lte: lte ? collection0_indexify(lte) : null
    })
  },
  encodeValue (version, record) {
    setVersion(version)
    return c.encode(collection0_enc, record)
  },
  trigger: null,
  reconstruct: collection0_reconstruct,
  reconstructKey: collection0_reconstruct_key,
  indexes: []
}

const collections = [
  collection0
]

const indexes = [
]

module.exports = { version, collections, indexes, resolveCollection, resolveIndex }

function resolveCollection (name) {
  switch (name) {
    case '@db/nested-members': return collection0
    default: return null
  }
}

function resolveIndex (name) {
  switch (name) {
    default: return null
  }
}
